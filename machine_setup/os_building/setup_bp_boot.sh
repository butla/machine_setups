# TODO mounting (if not mounted)
# build boot, endure systemd-boot is setup and grub uninstalled
# Use mount_bp? Make it idempotent? Use the common boot script, make that use hosts_config

# Use hosts_config.yml with yq
