#!/usr/bin/env python3
import getpass
import sys

password = getpass.getpass()
letter_positions = [int(position) for position in sys.argv[1:]]

for position in letter_positions:
    print(password[position - 1])
